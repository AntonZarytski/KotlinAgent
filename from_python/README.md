# Claude Chat Agent 🤖

Современный веб-чат с использованием Claude API (Anthropic) и Flask.

![Python](https://img.shields.io/badge/Python-3.9+-blue.svg)
![Flask](https://img.shields.io/badge/Flask-3.0.0-green.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Структура проекта](#структура-проекта)
- [Локальная разработка](#локальная-разработка)
- [Деплой на VPS](#деплой-на-vps)
- [Скрипты управления](#скрипты-управления)
- [Логирование и мониторинг](#логирование-и-мониторинг)
- [Troubleshooting](#troubleshooting)
- [Полезные ссылки](#полезные-ссылки)

## 🎯 Описание проекта

Веб-приложение для общения с Claude AI через удобный интерфейс чата. Включает:
- Современный адаптивный UI с анимациями
- REST API для взаимодействия с Claude
- Детальное логирование всех операций
- Автоматический деплой и мониторинг
- Systemd сервис для продакшн окружения

## 📁 Структура проекта

```
PythonAgent/
├── App.py                  # Основное Flask приложение
├── public/
│   └── index.html         # Веб-интерфейс чата
├── requirements.txt       # Python зависимости
├── .env                   # Переменные окружения (не в git!)
├── .gitignore            # Игнорируемые файлы
├── README.md             # Документация
│
├── deploy_local.sh       # Деплой локальных файлов на VPS
├── deploy_git.sh         # Деплой из Git репозитория
├── get_logs.sh           # Получение логов с сервера
└── setup_ssh.sh          # Настройка SSH автоматизации
```

## 🔗 Полезные ссылки

- **Hetzner Cloud Console**: https://console.hetzner.com/projects
- **Anthropic API Keys**: https://platform.claude.com/settings/workspaces/default/keys
- **VPS Server**: http://95.217.187.167:8000

---

## 💻 Локальная разработка

### Требования

- Python 3.9 или выше
- pip (менеджер пакетов Python)
- Git

### Установка

1. **Клонирование репозитория**

```bash
git clone https://github.com/AntonZarytski/ClaudAgentPY.git PythonAgent
cd PythonAgent
```

2. **Создание виртуального окружения**

```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

3. **Установка зависимостей**

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

**Зависимости проекта:**
- `flask==3.0.0` - веб-фреймворк
- `anthropic==0.39.0` - SDK для Claude API
- `python-dotenv==1.0.0` - загрузка переменных окружения
- `gunicorn==21.2.0` - WSGI сервер для продакшна
- `httpx==0.27.2` - HTTP клиент
- `httpcore==1.0.2` - HTTP ядро

### Настройка .env файла

Создайте файл `.env` в корне проекта:

```bash
# .env
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
PORT=8000
```

> ⚠️ **Важно:**
> - Файл `.env` содержит секретные данные и НЕ должен попадать в Git
> - Убедитесь, что `.env` добавлен в `.gitignore`
> - Получите API ключ на https://platform.claude.com/settings/workspaces/default/keys

### Запуск локально

```bash
source venv/bin/activate
python App.py
```

Приложение будет доступно по адресу:
- http://127.0.0.1:8000
- http://localhost:8000

**Проверка работы:**

```bash
# Health check
curl http://127.0.0.1:8000/health

# Тест chat endpoint
curl -X POST http://127.0.0.1:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"Привет!"}'
```

---

## 🚀 Деплой на VPS

### Информация о сервере

- **Провайдер**: Hetzner Cloud
- **ОС**: Ubuntu 24.04 LTS
- **Пользователь**: `agent`
- **IPv4**: `95.217.187.167`
- **IPv6**: `2a01:4f9:c012:a888::1`
- **Порт**: `8000`

### Первоначальная настройка сервера

1. **Подключение к серверу**

```bash
ssh root@95.217.187.167
```

2. **Установка необходимых пакетов**

```bash
sudo apt update
sudo apt install -y python3 python3-pip python3.12-venv git
```

3. **Создание пользователя (если нужно)**

```bash
sudo useradd -m -s /bin/bash agent
sudo usermod -aG sudo agent
```

4. **Клонирование проекта**

```bash
cd /home/agent
git clone https://github.com/AntonZarytski/ClaudAgentPY.git PythonAgent
cd PythonAgent
```

5. **Настройка виртуального окружения**

```bash
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

6. **Создание .env файла**

```bash
nano .env
```

Добавьте:
```env
ANTHROPIC_API_KEY=sk-ant-api03-your-production-key-here
PORT=8000
```

7. **Настройка systemd сервиса**

Создайте файл `/etc/systemd/system/pythonagent.service`:

```bash
sudo nano /etc/systemd/system/pythonagent.service
```

Содержимое:

```ini
[Unit]
Description=PythonAgent Gunicorn Service
After=network.target

[Service]
User=agent
Group=agent
WorkingDirectory=/home/agent/PythonAgent
Environment=PYTHONUNBUFFERED=1
ExecStart=/home/agent/PythonAgent/venv/bin/gunicorn -w 4 -b 0.0.0.0:8000 App:app
Restart=always

[Install]
WantedBy=multi-user.target
```

8. **Запуск сервиса**

```bash
sudo systemctl daemon-reload
sudo systemctl enable pythonagent
sudo systemctl start pythonagent
sudo systemctl status pythonagent
```

---

## 🛠 Скрипты управления

Проект включает набор скриптов для автоматизации деплоя и диагностики.

### Настройка SSH (один раз)

Для автоматизации SSH подключений без ввода пароля:

```bash
./setup_ssh.sh
```

Этот скрипт:
- Настраивает ssh-agent для автоматической разблокировки ключа
- Создает SSH config для удобного подключения
- Добавляет автозагрузку ssh-agent в shell

После настройки можно подключаться просто:
```bash
ssh hetzner-vps
```

### Деплой локальных файлов

Копирует файлы с локальной машины на сервер и перезапускает сервис:

```bash
./deploy_local.sh
```

**Что делает скрипт:**
1. Проверяет наличие необходимых файлов (App.py, .env, index.html)
2. Копирует файлы на сервер через scp
3. Проверяет корректность .env на сервере
4. Перезапускает systemd сервис pythonagent
5. Выполняет проверку работоспособности (health check и chat endpoint)

**Использование:**
```bash
cd /Users/anton/IdeaProjects/PythonAgent
./deploy_local.sh
```

### Деплой из Git репозитория

Обновляет код на сервере из Git и перезапускает сервис:

```bash
./deploy_git.sh
```

**Что делает скрипт:**
1. Подключается к серверу по SSH
2. Выполняет `git pull` в директории проекта
3. Сохраняет и восстанавливает .env файл
4. Проверяет изменения в requirements.txt и обновляет зависимости при необходимости
5. Перезапускает systemd сервис
6. Выполняет проверку работоспособности

**Использование:**
```bash
cd /Users/anton/IdeaProjects/PythonAgent
./deploy_git.sh
```

> 💡 **Рекомендация:** Используйте `deploy_git.sh` для регулярных обновлений, а `deploy_local.sh` - для быстрого тестирования локальных изменений.

### Получение логов

Показывает детальную информацию о состоянии сервера и логи:

```bash
./get_logs.sh [количество_строк]
```

**Что показывает скрипт:**
- Статус systemd сервиса pythonagent
- Список процессов Gunicorn
- Состояние порта 8000
- Health check результат
- Последние N строк из journalctl (по умолчанию 100)
- Содержимое app.log (если существует)
- Последние ошибки из логов
- Проверка .env файла
- Использование ресурсов (память, диск, CPU)
- Последние запросы к API

**Примеры использования:**
```bash
./get_logs.sh          # Последние 100 строк
./get_logs.sh 200      # Последние 200 строк
./get_logs.sh 50       # Последние 50 строк
```

### Просмотр логов в реальном времени

Для мониторинга логов в реальном времени:

```bash
ssh hetzner-vps 'journalctl -u pythonagent -f'
```

или

```bash
ssh root@95.217.187.167 'journalctl -u pythonagent -f'
```

---

## 📊 Логирование и мониторинг

### Структура логирования

Приложение использует многоуровневое логирование:

1. **Файл app.log** - детальные логи приложения
   - Расположение: `/home/agent/PythonAgent/app.log`
   - Формат: `%(asctime)s - %(name)s - %(levelname)s - %(message)s`
   - Уровень: INFO и выше

2. **Systemd журнал** - логи сервиса
   - Просмотр: `journalctl -u pythonagent`
   - Включает stdout и stderr от Gunicorn

### Типы логируемых событий

- ✅ Успешная инициализация API клиента
- 📨 Входящие запросы на /api/chat
- 💬 Сообщения пользователей
- 🤖 Ответы от Claude API
- ❌ Ошибки аутентификации
- ⚠️ Превышение лимитов API
- 🔌 Проблемы с соединением
- 🐛 Неожиданные исключения

### Команды для просмотра логов

```bash
# Последние 100 строк логов сервиса
journalctl -u pythonagent -n 100

# Логи в реальном времени
journalctl -u pythonagent -f

# Логи за последний час
journalctl -u pythonagent --since "1 hour ago"

# Только ошибки
journalctl -u pythonagent -p err

# Файл app.log
tail -f /home/agent/PythonAgent/app.log
```

### Health Check Endpoint

**URL:** `http://95.217.187.167:8000/health`

**Ответ:**
```json
{
  "status": "ok",
  "timestamp": "2025-12-01T08:53:51.731353",
  "api_key_configured": true
}
```

### Мониторинг ресурсов

```bash
# Использование памяти процессами Gunicorn
ps aux | grep gunicorn

# Общее использование памяти
free -h

# Использование диска
df -h

# Нагрузка на CPU
top -b -n 1 | head -20
```

---

## 🔧 Troubleshooting

### Проблема: HTTP 500 Internal Server Error

**Симптомы:**
- Запросы к `/api/chat` возвращают ошибку 500
- В логах: `"Could not resolve authentication method"`

**Причина:** Отсутствует или неправильно настроен файл `.env` с `ANTHROPIC_API_KEY`

**Решение:**
```bash
# Подключитесь к серверу
ssh root@95.217.187.167

# Проверьте наличие .env
cd /home/agent/PythonAgent
ls -la .env

# Если файла нет, создайте его
nano .env

# Добавьте:
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
PORT=8000

# Перезапустите сервис
sudo systemctl restart pythonagent
```

### Проблема: Сервис не запускается

**Проверка статуса:**
```bash
systemctl status pythonagent
journalctl -u pythonagent -n 50
```

**Возможные причины:**
1. **Ошибка в коде** - проверьте логи на наличие Python исключений
2. **Порт занят** - проверьте: `ss -tlnp | grep 8000`
3. **Нет прав на файлы** - проверьте владельца: `ls -la /home/agent/PythonAgent`
4. **Виртуальное окружение повреждено** - пересоздайте venv

**Решение:**
```bash
# Остановите сервис
sudo systemctl stop pythonagent

# Проверьте порт
sudo lsof -i :8000
# Если порт занят, убейте процесс
sudo kill -9 <PID>

# Проверьте права
sudo chown -R agent:agent /home/agent/PythonAgent

# Запустите снова
sudo systemctl start pythonagent
```

### Проблема: WORKER TIMEOUT в логах

**Симптомы:**
```
[CRITICAL] WORKER TIMEOUT (pid:4271)
Worker exiting (pid: 4271)
```

**Причина:** Запрос к Claude API занимает слишком много времени

**Решение:**
Увеличьте timeout в systemd сервисе:
```bash
sudo nano /etc/systemd/system/pythonagent.service
```

Измените ExecStart:
```ini
ExecStart=/home/agent/PythonAgent/venv/bin/gunicorn -w 4 -b 0.0.0.0:8000 --timeout 120 App:app
```

Перезагрузите:
```bash
sudo systemctl daemon-reload
sudo systemctl restart pythonagent
```

### Проблема: SSH требует ввод пароля каждый раз

**Решение:**
```bash
# Запустите скрипт настройки SSH
./setup_ssh.sh

# Или вручную добавьте ключ в ssh-agent
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
```

### Проблема: Изменения в коде не применяются

**Причина:** Забыли перезапустить сервис после деплоя

**Решение:**
```bash
# После любых изменений в коде ВСЕГДА перезапускайте сервис
sudo systemctl restart pythonagent

# Или используйте скрипты деплоя, которые делают это автоматически
./deploy_local.sh
# или
./deploy_git.sh
```

### Проблема: Превышен лимит API запросов

**Симптомы:**
- Ошибка 429 в логах
- Сообщение: "Rate limit exceeded"

**Решение:**
1. Проверьте лимиты на https://platform.claude.com/
2. Подождите несколько минут
3. Рассмотрите возможность увеличения лимитов или оптимизации запросов

### Полезные команды для диагностики

```bash
# Проверка всех компонентов
./get_logs.sh

# Проверка процессов
ps aux | grep gunicorn

# Проверка портов
ss -tlnp | grep 8000

# Проверка памяти
free -h

# Проверка диска
df -h

# Тест API вручную
curl -X POST http://localhost:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"test"}'
```

---

## 📚 Дополнительная информация

### Управление systemd сервисом

```bash
# Запуск
sudo systemctl start pythonagent

# Остановка
sudo systemctl stop pythonagent

# Перезапуск
sudo systemctl restart pythonagent

# Статус
systemctl status pythonagent

# Включить автозапуск
sudo systemctl enable pythonagent

# Отключить автозапуск
sudo systemctl disable pythonagent

# Перезагрузка конфигурации
sudo systemctl daemon-reload
```

### Работа с Git на сервере

```bash
# Проверка текущей ветки
git branch

# Проверка изменений
git status

# Получение обновлений
git fetch
git pull

# Просмотр последних коммитов
git log --oneline -10

# Откат к определенному коммиту
git checkout <commit-hash>
```

### Безопасность

- ✅ Файл `.env` НЕ должен быть в Git
- ✅ Используйте разные API ключи для dev и production
- ✅ Регулярно обновляйте зависимости: `pip install --upgrade -r requirements.txt`
- ✅ Мониторьте логи на предмет подозрительной активности
- ✅ Настройте firewall для ограничения доступа к порту 8000

---

## 📝 Лицензия

MIT License

---

## 👤 Автор

Anton Zarytski

---

**Последнее обновление:** 2025-12-01
