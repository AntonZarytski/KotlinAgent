<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Консольный аппендер -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Файловый аппендер для app.log (в корне проекта) -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>app.log</file>
        <encoder>
            <!-- Упрощенный формат: время (без даты) + logger + сообщение -->
            <pattern>%d{HH:mm:ss.SSS} %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
            <!-- При ротации не сохраняем старые логи (maxIndex=0) -->
            <fileNamePattern>app.log.%i</fileNamePattern>
            <minIndex>1</minIndex>
            <maxIndex>0</maxIndex>
        </rollingPolicy>
        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <!-- ~1000 строк * 100 символов = 100KB -->
            <maxFileSize>100KB</maxFileSize>
        </triggeringPolicy>
    </appender>

    <!-- Отдельный файл для ошибок -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>app-error.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>app-error.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- Уровень логирования для приложения -->
    <logger name="com.claude.agent" level="DEBUG"/>

    <!-- Уровень логирования для Ktor -->
    <logger name="io.ktor" level="INFO"/>

    <!-- Корневой logger -->
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
        <!-- FILE и ERROR_FILE будут добавлены программно, если ENABLE_FILE_LOGGING=true -->
        <appender-ref ref="FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>
</configuration>

