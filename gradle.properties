# Enable the build cache to save time by reusing outputs produced by other successful builds.
# https://docs.gradle.org/current/userguide/build_cache.html
org.gradle.caching=true
# Enable the configuration cache to reuse the build configuration and enable parallel task execution.
# (Note that some plugins may not yet be compatible with the configuration cache.)
# https://docs.gradle.org/current/userguide/configuration_cache.html
org.gradle.configuration-cache=true

# JVM arguments for Gradle daemon
org.gradle.jvmargs=-Xmx2g --add-opens=java.base/java.lang=ALL-UNNAMED --enable-native-access=ALL-UNNAMED

# Application run arguments
org.gradle.application.jvmargs=--enable-native-access=ALL-UNNAMED

# ?????????? Java 24 (??? ??????? ? CLAUDE.md)
org.gradle.java.home=/Users/anton/Library/Java/JavaVirtualMachines/corretto-24.0.2/Contents/Home

# Disable incremental compilation for Kotlin/JS to avoid internal compiler errors
kotlin.incremental.js.ir=false

# Kotlin/JS ???????????
kotlin.js.compiler=ir
kotlin.js.generate.executable.default=true

# ?????????? warnings
org.gradle.warning.mode=none